<nav class="navbar navbar-expand-lg bg-secondary">
  <div class="container-fluid">
  <%=link_to image_tag('logo.png',width:"8%"),home_path %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
   <%= form_with(url: '/home', method: 'get', local: true) do %>
  <%= text_field_tag(:search) %>
  <%= submit_tag("Search") %>
<% end %>
<%= link_to "Advanced Search", new_search_path, class: "nav-link" %>
	<li class="nav-item">
	<%=link_to "About" , about_path , class: "nav-link" %>
        </li>
	 <% if user_signed_in? %>
            <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            My workspace
          </a>
          <ul class="dropdown-menu">
           <li><%=link_to "My Blogs" , myblogs_path %></li>
            <li> <%= link_to 'My Favorites', favs_path %> </li>
          </ul>
        </li>
          <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
<%= image_tag 'user.png', :width => "20%"  %>
          </a>
          <ul class="dropdown-menu">
            <li> 
                       <%= link_to "Hi, #{current_user.email}", edit_user_registration_path,
                        class: "nav-link #{active_class(edit_user_registration_path)} dropdown-item" %>
           </li>         
            <li> 
                       <%= link_to "My Notifications", home_path,
                        class: "nav-link dropdown-item" %>
           </li>              <li><hr class="dropdown-divider"></li>
            <li>
                      <%= button_to 'Sign Out', destroy_user_session_path,
                        method: :delete, data: { turbo: false }, class: 'dropdown-item' %>
            </li>          </ul>
          <ul class="dropdown-menu">
          </li>
     <% else %>
            <%= link_to 'Sign In', new_user_session_path,
                        class: "nav-link #{active_class(new_user_session_path)} btn btn" %>
          
            <%= link_to 'Sign Up', new_user_registration_path,
                        class: "nav-link #{active_class(new_user_registration_path)} btn btn" %>
     <% end %>
      </ul>
    </div>
  </div>
</nav>
